// config.js — Demo admin credentials (insecure: visible in repo)
// Change these values if you want different credentials.
const ADMIN_CREDENTIALS = {
  username: 'admin',
  password: 'secret123'
};
